apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nightawk-benchmark-route-http-{{ item }}
  namespace: "{{ operator_namespace }}"
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: nightawk-benchmark-service-http-{{ item }}