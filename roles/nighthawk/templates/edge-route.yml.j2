apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nighthawk-benchmark-route-edge-{{ item }}
  namespace: "{{ operator_namespace }}"
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: nighthawk-benchmark-service-edge-{{ item }}