---
es: "{{ elasticsearch|default([]) }}"
es_server: "{{ es.server|default('') }}"
es_port: "{{ es.port|default(9200) }}"
es_index: "ripsaw-fio"
test_user: "ripsaw"
pin_server: "{{ workload_args.pin_server|default('') }}"
ceph_cache_drop_pod_ip: "{{ workload_args.rook_ceph_drop_cache_pod_ip|default('') }}"
