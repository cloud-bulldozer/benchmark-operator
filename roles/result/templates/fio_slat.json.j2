{
  "uuid": "{{ uuid }}",
  "user": "{{ user }}",
  "time_ms": {{ ((fio_end_time | int) - ((((workload.args.runtime | int) * (4 - (fio_jobsequence|int))) + ((workload.args.ramp_time | int) * (3 - (fio_jobsequence|int)))) * 1000) ) + (item.split(', ')[0]| int)  }},
  "latency": {{ item.split(', ')[1]| int }},
  "data_direction": {{ item.split(', ')[2]| int }},
  "offset": {{ item.split(', ')[3]| int }},
  "log_file": "slat",
  "job_sequence": "{{ fio_jobsequence }}"
}
