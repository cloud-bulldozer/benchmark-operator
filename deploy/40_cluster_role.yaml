apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: benchmark-operator
rules:
- apiGroups: [""]
  resources:
    - pods
  verbs:
    - get
    - delete
    - create
    - update
    - patch
