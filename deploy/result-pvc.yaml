---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
    name: bench-results
spec:
    accessModes:
      - ReadWriteMany
    storageClassName: standard
    resources:
        requests:
            storage: 1Gi
