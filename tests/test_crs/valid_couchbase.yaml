apiVersion: benchmark.example.com/v1alpha1
kind: Benchmark
metadata:
  name: example-benchmark
spec:
  cleanup: false
  couchbase:
    servers:
      size: 1
    storage:
      use_persistent_storage: False
