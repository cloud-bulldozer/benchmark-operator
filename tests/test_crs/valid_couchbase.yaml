apiVersion: benchmark.example.com/v1alpha1
kind: Benchmark
metadata:
  name: test-couchbase
  namespace: ripsaw
spec:
  cleanup: false
  infrastructure:
    name: couchbase
    args:
      servers:
        size: 1
      storage:
        use_persistent_storage: False
