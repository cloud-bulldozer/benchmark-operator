apiVersion: benchmark.example.com/v1alpha1
kind: Benchmark
metadata:
  namespace: ripsaw
  name: test-byowl
spec:
  workload:
    name: byowl
    args:
      image: "quay.io/jtaleric/uperf:testing"
      clients: 1
      commands: "echo Test"
